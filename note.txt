PA2:

* the client program will spawn one customer thread, and the customer thread will send 128 read
request to the server where the customer id varies from 0 to 128,
; isn't it from 0 to 127?

* Should I also ship laptop when the read request?

* Sending identifier to the server; should I send it with the message? or can I send it in two times?; two identifier comes at the same time

* How do IFA receive the message from the Server?

* Configure the client instance to only send to one server?

* Is it invarialbe that 
x x x x-1
pl, pc, il, ic 

* Write the request to the req.last_index... 

* can I use api and other libraries for the final assignmnet?